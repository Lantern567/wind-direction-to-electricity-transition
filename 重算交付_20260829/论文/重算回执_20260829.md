# 重算回执（ADAPEN_v41 重算与确认清单 · 2026-08-29）

> 执行：cream（实习生侧）｜日期：2026-08-29
> 对应清单：`ADAPEN_v41_重算与确认清单_20260829.md`
> 全部数字实算，来源脚本与输出文件路径见各条。P0 已裁决、P1 全部落盘、P2-1 新算完成。

---

## 三个判断题（先给结论）

| # | 问题 | 答案 |
|---|---|---|
| 1 | 逐场越线是 5 个还是 4 个？（P0-1） | **4 个**（57, 66, 91, 157；≥3 年加 155 共 5 个）。F160 比值 1.03→0.96 掉线。比值区间 1.14–3.19。 |
| 2 | 建成尾流损失与 A 的相关，权威值 −0.045 还是 −0.10？（P1-4） | **都不是**。冻结管线可复现的权威值为 **ρ ≈ −0.03**（真实排布、多年平均、n=171、p=0.70）；终期年份口径 +0.02。−0.045 与 −0.097 在仓库中均无计算脚本，且按所列口径均无法复现。所有口径下 \|ρ\| ≤ 0.1、p > 0.2，"几乎不相关"的定性结论稳健。 |
| 3 | 东亚/欧洲格点中位 1.11/0.37 还是 1.28/0.40？（P1-8） | **1.28 / 0.40**（学长口径）。两套值都精确复现了：旧值来自旧图 3 脚本更宽的 bbox（东亚 lon 100–130 / lat 2–36、欧洲 lon −10–30 / lat 36–62），新值来自窄 bbox（101–126 / 5–33、−2–23 / 35–61）。差异纯属范围口径，正文采用 1.28/0.40 正确。 |

---

## P0 裁决（2 项）

### P0-1　逐场越线：5 个 → **4 个**

**裁决**：4 个（57, 66, 91, 157；≥3 年 5 个含 155）。F160（n=9, G=2.444%, M_S_std=2.556%）比值 0.96，不越线。

**差异来源已定位**：清单中两行的差异完全来自 v5→v6 的 FLORIS 修复（P10 全部 FLORIS 逐时自算 ×10 轮、P01/P00 逐时精确功率插值）→ 只改变 M_S_std，G 不变（G 出自 task3 S3/S1 口径）：

| 场 | G | M_S_std v5→v6 | 比值 v5→v6 |
|---|---|---|---|
| 57 | 18.216% | 6.397→5.719 | 2.85→3.19 |
| 66 | 7.763% | 3.905→3.716 | 1.99→2.09 |
| 91 | 6.617% | 6.207→5.818 | 1.07→1.14 |
| 155 | 9.568% | 4.616→4.316 | 2.07→2.22 |
| 157 | 6.928% | 4.189→4.535 | 1.65→1.53 |
| **160** | 2.444% | 2.363→2.556 | **1.03→0.96** |

**说明**：`分析材料_朝向长尾走廊/figures/_build_v6_derived.py` 在仓库中**不存在**（该目录已改名为 `分析材料_几何主导杠杆`，figures/ 下只有 Fig6_windspeed_role.py 与 nc_style.py，无任何 `_build_v6_derived.py`；git 历史中也无此文件）。红线中"派生量一律经 figures/_build_v6_derived.py 重算"的要求从未落地。本次以完全相同的方式重做了这件事：新建 `四场景风速风向分解贡献/run_floris_decomposition_v6.py`（与 v5 分解脚本逐行同逻辑，仅输入换为 `four_scenario_floris_aep_v6.csv`，P11 仍取 S3、G 仍取 S1/S3），所得结果与清单第二行**完全一致**（4 | 5 | 1.14–3.19）。

**受影响位置（正文改为 4 个后需同步）**：摘要、2.1-P2、2.1-P3、图 1b/c/d、结论 C1、方法 5.8（"对 6 个逐场越线风场"→ 5 场：57/66/91/155/157，F160 移出机理诊断。wp9g_windwindow_diagnosis.csv 中 F160 的数字仍有效，仅集合口径变化）。此外 5/108=4.6% 需改为 4/108≈3.7%，6/146 改为 5/146。

**验收产物**：
- 权威表：`四场景风速风向分解贡献/output/four_scenario_farm_summary_AUTHORITATIVE.csv`（171 行，与 v6 派生表同内容）
- v6 派生表：`四场景风速风向分解贡献/output/four_scenario_farm_summary_v6.csv`、`four_scenario_effects_farmyear_v6.csv`、`four_scenario_aep_farmyear_v6.csv`
- 脚本：`四场景风速风向分解贡献/run_floris_decomposition_v6.py`

### P0-2　wp1_geometry_frozen.csv

**已落盘**：`结论三、四重算结果/output-new/wp1_geometry_frozen.csv`（171 行；由 `补算/wp1_local_geometry.py` 生成、与 `结论三、四重算结果/wp1_local_geometry.py` 同源）。重算与 CSV 逐场一致（0/171 差 > 0.0005）。

**与学长 KD-tree 临时表的比对报告**（学长方法：终期机位 (x_m, y_m) UTM 坐标 → cKDTree 最近邻中位 / 198 m；冻结表方法：终期机位 lon/lat → 逐场 AEQD 等距方位投影 → cKDTree 最近邻中位 / 198 m）：
- 两种方法逐场相关 r = 0.99995，相对差中位 0.03%；
- **容差 1e-6 无法满足**：UTM 是保形投影（比例因子 0.9996，且个别场分带偏移），与 AEQD 真距存在系统性小差——最大绝对差 0.174 D（65 场 > 1e-3）。这是投影口径差异，不是计算错误；
- 头部数字两种方法都过：UTM 口径 ρ(spacing_D_med, A) = **−0.747**（与学长报告值一致）；冻结 AEQD 口径 −0.749。≥4D 48 场 0 高响应、<4D 123 场 27 高响应，两法一致。
- 比对明细：`补算/output/wp1_spacing_method_comparison.csv`

---

## P1 重算落盘（7 项）

### P1-1　风频权重 W 与 R1/R2（已解决，171 场全覆盖）

- W 数组：`output-new/wp3_climate_weights.npz`（键 W (171,18,72) float32、farm_ids、ws、wd；2014–2024 逐时聚合、小时数加权，逐场 Σ=1）
- 集中度表：`output-new/wp3_farm_concentration.csv`（farm_id, R1_freq, R2_freq, R1_energy, R2_energy；171 场全覆盖，旧 WCI 仅 72 场的问题不复存在）
- 三数复核：
  - ρ(R2_freq, A) = **+0.610**（p=8.8e-19）；ρ(R2_energy, A) = +0.583（等年权重口径 +0.584，与 wp9c 报告一致）→ 落在 0.58–0.63 区间 ✓
  - 控制 S 后偏相关 ρ(A, R2_freq | S) = **+0.589**（p=3.0e-17）✓（正文 0.59）
  - R1 无区分力：ρ(R1_freq, A)=+0.057、ρ(R1_energy, A)=−0.051，Mann-Whitney p=0.7 ✓
  - 分层表复现：密排×高轴向 22/67=32.8% vs 密排×低轴向 5/56=8.9%；疏排 0/48 ✓

### P1-3　Δp90 尾流端元（已重算落盘）

- 按方法 5.4 重算：171 场终期排布 × 逐场 TI × iea_10MW × gauss，9.0 m/s × 72 风向 FLORIS 逐台扫描（turbine_average_velocities），逐台速度亏损 δ=1−v/U，每方向逐台 p90 与均值；**高损失方向取逐台 p90 亏损最大者、低损失方向取最小者**（该口径最接近主稿 0.790——若按 η 取方向得 0.719，按平均亏损取方向得 0.757）；Δp90 = p90(高) − p90(低)。
- 交叉检验：本扫描 η 由逐台速度按参考功率曲线重算（与 FLORIS 场功率同一物理定义），与 wp7a_real_eta 的 9 m/s 档逐格最大差 1.2%（功率插值口径差异）。
- 输出：`output-new/wp4_wake_endmember.csv`（清单 8 列）+ 全量审计表 `补算/output/wp4_wake_endmember_full.csv`（171×72 = 12,312 行）
- ρ(Δp90, A) 复核：**+0.796（p = 9.8×10⁻³⁹，n = 171）**；10° 子网格口径 +0.804。与主稿 0.790（p = 5.8×10⁻³⁷）同量级、同方向——原始 0.790 的脚本不在仓库，其细微口径（速度源/网格偏移）不可考；建议主稿保留 0.790 或改为复核值 0.796，由学长定。
- 附加验证：9 m/s 方向效率跨度（学长图 2f 替代量）按 η 极值方向复算 ρ = +0.645，与学长 0.644 一致 ✓
- 脚本：`补算/wp4_wake_endmember.py`（断点续跑，扫描检查点 `补算/output/wp4_endmember_scan.npz`）

### P1-4　建成尾流损失口径（判断题 2，见上）

口径矩阵（全 Spearman × A_wp7a，除非注明）：

| 尾流损失口径 | n | ρ |
|---|---|---|
| s4_balanced_gauss 真实排布·终期年份 | 171 | +0.019（p=0.81） |
| s4_balanced_gauss 真实排布·多年平均 | 171 | **−0.029（p=0.70）** |
| 同上·限 n_turb≥10 的 155 项目·多年平均 | 155 | −0.094 |
| s4 s1_opt 排布·终期年份 | 171 | −0.078 |
| 六范式模板建成朝向尾流损失均值 | 171 | +0.519（正相关） |
| wp7a η 建成朝向气候加权尾流损失 | 171 | +0.175 |

**结论**：−0.045 无脚本来源且不可复现；学长 −0.097 最接近的组合是 155 项目子样本 + 多年平均（−0.094，非"终期年份"）。所有口径 |ρ| ≤ 0.1、p > 0.2。建议正文改为可复现口径：**ρ = −0.03（真实排布、建成朝向、多年平均、n=171、p=0.70）**，或保守表述"|ρ| ≤ 0.1（不显著）"。正文现用 −0.10 的定性含义（几乎不相关）不受影响。
- 脚本：`补算/_tx_recheck_p14_wakeloss.py`、`补算/_tx_recheck_p14_grid.py`

### P1-5　二阶谐波重构（已落盘，四统计量全部复现）

- `output-new/wp6_harmonic_reconstruction.csv`（farm_id, A, phi_opt_deg, dphi_deg, G_recon_2024, G_sim_2024；171 行）
- 四统计量：Spearman **0.773** / Pearson **0.776** / MAE **0.96 pp** / bias **+0.66 pp** —— 与清单逐字一致 ✓
- 相位—兑现（27 场高响应）：口径甲（正文，2014–2024 样本外 G）ρ = **0.861** 复现；口径乙（E_pool 同气候期 G）ρ = 0.864。学长 0.874 与口径乙差 0.010，来自其 E_pool 重算的细微口径差。**建议正文保留 0.861**（样本外口径更符合"兑现"的语义）。
- 脚本：`补算/_tx_recheck_p15_harmonic.py`（与 wp9e 同口径）

### P1-6　形状—风况联合模型（已落盘）

- `output-new/wp5_shape_climate_model.csv`（秩 OLS，X 秩变换、y=log A 秩）：

| model | r2_in_sample | r2_cv5 | r2_loco |
|---|---|---|---|
| S+R2_e | 0.693 | 0.683 | 0.615 |
| S+R2_e+ws | **0.775** | 0.762 | 0.708 |
| S+R2_e+ws+R1_e | 0.775 | 0.758 | 0.674 |

- 样本内 0.775 ≈ 正文 0.76 ✓（复现 wp9c 报告 ⑥ 的 0.775）。**LOCO 0.622 无法精确复现**：国家整组留出在冻结口径下为 0.615 / 0.708 / 0.674（按模型规格）；随机五折 0.762。0.622 位于 0.615–0.708 之间，定性结论（整组留出显著低于样本内）成立。建议正文把 LOCO 值改为可复现的 **0.615（S+R2_e 口径）** 或 0.708（S+R2_e+ws 口径），或向学长确认其 0.622 的模型规格。

### P1-7　方差分解（已机读化）

- `output-new/wp5c_variance_decomposition.csv`：气候(场址) 60.72% / 建设范式 19.27% / 场址×范式 20.01%（SS 1116.5645 / 354.3117 / 367.9188，与 wp5c_anova.txt 逐字一致）

---

## P1-8　格点响应中位（判断题 3，见上）

两套 bbox + 海陆掩膜 + valid 标志全部实测复现：

| bbox | 东亚 | 欧洲 | 来源 |
|---|---|---|---|
| 学长（101–126 / 5–33；−2–23 / 35–61） | **1.28%** (n=182) | **0.40%** (n=202) | 精确复现 ✓ |
| 旧图3（100–130 / 2–36；−10–30 / 36–62） | 1.11% (n=224) | 0.37% (n=305) | 精确复现 ✓ |

- 海陆掩膜必要：不加掩膜时学长 bbox 为 1.96/0.67（陆上格点抬高）。
- 数据源：`output-new/wp5c_cross_grid.npz` 的 A (1446×6) 六范式均值。
- 脚本：`补算/_tx_recheck_p18_gridmedian.py`

---

## P2-1　11.8D 模板下项目级朝向价值（已新算）

- `output-new/wp7e_orientation_value_by_template.csv`（155 项目 × 6 模板 = 930 行；farm_id, template, dE_GWh_orientation, pct_orientation, A_pct, E_rot_mean_kWh_per_turb）
- 口径：E_pool (171,6,72) kWh/台·年（wp5c 交叉仿真，场址气候 × 64 机位模板），dE = max_θ − mean_θ（72 方向，等价于 A 的半圆窗口口径）；项目级 × n_turb。交叉检验：pct_orientation 与 wp5c A 逐格最大差 0.011 pp ✓
- 结果（155 项目合计 / 中位）：

| 模板 | dE 中位 GWh/yr | dE 合计 GWh/yr | pct 中位 |
|---|---|---|---|
| S_A (Aligned) | 0.008 | 1.96 | 0.22% |
| S_B0/S_B45 (Dense) | 0.021 | 6.86 | 0.83% |
| S_C | 0.018 | 4.63 | 0.54% |
| S_D | 0.022 | 6.64 | 0.78% |
| **S_E (11.8D)** | **0.006** | **1.37** | **0.15%** |

- **S_E（最宽松 11.8D）下**：走廊 20 项目 pct 中位 **0.86%** vs 其他 135 项目 **0.14%**（约 6×，与正文"仍为其他海域 5.5 倍"的范式级口径呼应；项目级口径下比例更高）。全部 155 项目合计 1.37 GWh/yr——朝向自由度在模板整体替换后仍保留可测的项目级价值，且集中在走廊。
- 脚本：`补算/wp7e_orientation_value.py`

---

## P3 体例（3 项，非计算项，转学长）

- P3-1 引文：需 Crossref DOI 核验后插入——本侧无新增计算。
- P3-2 引用格式：按首次出现顺序编号的上标——排版事务，学长侧定。
- P3-3 Zenodo 数据/代码：可协助打包，待学长给仓库名与冻结清单（本回执的 output-new/ 8 个新文件 + AUTHORITATIVE 表即为候选冻结清单）。

---

## 附：废弃 v5 数据清理（2026-08-29 补）

按"废弃数据该删就删"原则清理：
- 删除（git rm）：`四场景风速风向分解贡献/output/` 下 v5 派生的
  `four_scenario_farm_summary.csv`、`four_scenario_effects_farmyear.csv`、
  `four_scenario_aep_farmyear.csv`、`four_scenario_threshold_farms.csv`、
  `probability_checks.csv`、`run_log.csv` 六个文件；
- 删除：`figures-nat/FigS3_sensitivity_v5口径备份.png`；
- 图 1 / 图 S2 构建器数据源切换到 `four_scenario_farm_summary_AUTHORITATIVE.csv`，
  越线集断言更新为 [57,66,91,157]+155，F160 相关引用（标注/打印/断言）全部移除，
  重建 PNG（Fig1_nat.png 已无 F160 星标）；图 S3 已按 v6 口径重建；
- SI v4 表 S8a 移除 v5 旧表行（25→24 文件），注改"v5 派生表已删除"，docx 已重建；
- 全仓扫描：`.py`/`.md` 中仅剩描述性引用（README、实习生任务书、历史脚本 v3 构建器、
  清单、补丁脚本），无可执行的数据读取引用残留。v5 数据可从 git 历史恢复，不丢失。

## 交付物索引（全部已落盘）

```
结论三、四重算结果/output-new/
  wp1_geometry_frozen.csv                 (P0-2)
  wp3_climate_weights.npz                 (P1-1)
  wp3_farm_concentration.csv              (P1-1)
  wp4_wake_endmember.csv                  (P1-3)
  wp5_shape_climate_model.csv             (P1-6)
  wp5c_variance_decomposition.csv         (P1-7)
  wp6_harmonic_reconstruction.csv         (P1-5)
  wp7e_orientation_value_by_template.csv  (P2-1)
四场景风速风向分解贡献/output/
  four_scenario_farm_summary_AUTHORITATIVE.csv  (P0-1)
  four_scenario_farm_summary_v6.csv / effects_v6 / aep_v6  (P0-1 派生)
```

外加本回执。未决事项：
1. 正文 5.8 的机理诊断集合 6→5 场（F160 移出）。
2. P1-4 的 −0.10 是否改回可复现的 −0.03（或 |ρ|≤0.1 表述）。
3. P1-6 的 LOCO 0.622 用哪个可复现值替换（0.615 / 0.708）。
4. P1-5 相位 0.861 与 0.874 统一取哪值（建议 0.861，样本外口径）。
